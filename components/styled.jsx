import styled from 'styled-components';

const Layout = styled.div`
  text-align: left;
  display: flex;
  align-items: center;
  justify-content: center;
`;

const Wrapper = styled.div`
  background: #fff159;
`;

const LogoWrapper = styled.div`
  cursor: pointer;
`;

export { Layout, Wrapper, LogoWrapper };
